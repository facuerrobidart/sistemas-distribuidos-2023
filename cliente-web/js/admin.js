'use strict';

(function () {
    function init() {
        var router = new Router([
            new Route('ascensores', 'gestion-ascensores.html', true)           
        ]);
    }
    init();
}());